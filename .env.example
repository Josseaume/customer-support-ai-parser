# Copy this file to `.env.local` and fill in your own key (never commit the real key).
# Create a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Optional — override the model (defaults to openai/gpt-oss-120b:free)
OPENROUTER_MODEL=
